<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ProVoiceLedger.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- 🔹 Basic navigation shell with two pages (no icons, no templates) -->
    <TabBar>
        <ShellContent Title="Record" ContentTemplate="{DataTemplate ContentPage}" />
        <ShellContent Title="History" ContentTemplate="{DataTemplate ContentPage}" />
    </TabBar>
</Shell>
